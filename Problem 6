#include <iostream>
using namespace std;

int main ()
{
	int square_sum = 0;
	int sum_square = 0;
	for (int i = 0; i <= 100; i++)
	{
		sum_square += i * i;
		square_sum += i;
	}
	square_sum *= square_sum;
	cout << square_sum - sum_square << endl;
	return 0;
}
